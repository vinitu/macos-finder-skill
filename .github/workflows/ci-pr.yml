name: CI · PR

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate structure
        run: |
          set -euo pipefail
          for path in AGENTS.md LICENSE README.md SKILL.md Makefile scripts/folder/list.applescript tests/dictionary_contract.sh tests/smoke_finder.sh; do
            [ -f "$path" ] || { echo "::error::Missing $path"; exit 1; }
          done
      - run: make compile
      - run: |
          if command -v shellcheck >/dev/null 2>&1; then shellcheck tests/*.sh; fi
      - name: SKILL frontmatter
        run: |
          head -1 SKILL.md | grep -q '^---' && grep -q '^name:' SKILL.md && grep -q '^description:' SKILL.md
      - run: bash tests/dictionary_contract.sh
      - run: make test
